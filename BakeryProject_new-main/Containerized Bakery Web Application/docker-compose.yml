services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8080:8080"
    environment:
      # --- FOR CLOUD SETUP: Using your RDS Endpoint ---
      DB_HOST: database-2.ctoysy0yovt5.ap-southeast-1.rds.amazonaws.com
      DB_USER: admin
      DB_PASS: ganeshjadhav
      DB_NAME: bakery_db
      DB_SSL: 'true'
      ADMIN_USER: admin
      ADMIN_PASS: admin123
    restart: always

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
